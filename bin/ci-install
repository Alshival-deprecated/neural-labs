#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname -- "${BASH_SOURCE[0]}")/.."
for package in console control-plane mcp workspace workspace/desktop workspace/vendor/minipaint tools/ci; do
  npm ci --prefix "$package" --include=dev
done
